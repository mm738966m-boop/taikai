# 🎂 パーソナリティ お誕生日フォーム セットアップ手順

`birthday.html` から送信された内容を Googleスプレッドシートに自動でためる仕組みです。

## 公開URL（先方に配る）
https://mm738966m-boop.github.io/taikai/birthday.html

## 集計スプレッドシート
https://docs.google.com/spreadsheets/d/1-z6Cd-sNzYYnqkYT7BSKEGW9MPgU02dxMf0rRITjQB0/edit

---

## GAS（Apps Script）コード ＝ 賢い版

このコードは **スプシのヘッダーを見て、無い項目は列を自動で追加**します。
そのため、今後フォームに項目を足しても **GASの更新は不要**です。

```javascript
function doPost(e) {
  var data = JSON.parse(e.postData.contents);
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();

  // フォームで使う項目（順番＝列の順番）
  var fields = ['受付日時','パーソナリティ名','チャンネルURL','生年月日',
                'プレゼントに欲しいもの','誕生日の思い出','ひとこと','寅の会リスナーにひとこと'];

  // 見出し行が無ければ作成
  if (sheet.getLastRow() === 0) sheet.appendRow(fields);

  // 現在の見出しを取得し、足りない項目は列を自動追加
  var headers = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
  fields.forEach(function(f) {
    if (headers.indexOf(f) === -1) {
      sheet.getRange(1, headers.length + 1).setValue(f);
      headers.push(f);
    }
  });

  // 見出しの並びに合わせて1行追記
  var row = headers.map(function(h) {
    if (h === '受付日時') return new Date();
    return data[h] || '';
  });
  sheet.appendRow(row);

  return ContentService.createTextOutput('ok');
}
```

---

## 項目を変えたとき（＝今回）の更新手順

GASのコードを上の「賢い版」に貼り替えたら、**URLを変えずに**再反映します：

1. スプシ → `拡張機能 → Apps Script`
2. コードを上の「賢い版」に貼り替え → 💾 保存
3. `デプロイ → デプロイを管理` → 右上の **鉛筆（編集）**
4. **バージョン**を「**新バージョン**」にして **デプロイ**
   → `/exec` のURLは変わりません

> ※ 一度この「賢い版」にしておけば、次回以降フォームに項目を足しても
>   この再反映すら不要になります（列が自動で増えます）。
