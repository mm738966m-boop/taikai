# 🎂 パーソナリティ お誕生日フォーム セットアップ手順

`birthday.html`（入力 → 確認 → 完了 の3画面フォーム）から送信された内容を、
Googleスプレッドシートに自動でためていくための手順です。
退会届フォーム（`index.html`）とまったく同じ「自己完結HTML → GASでスプシ集計」方式です。

---

## ✅ スプレッドシートは作成済み

集計用スプレッドシートは作成済みです（見出し行も入力済み）：

**パーソナリティ誕生日リスト**
https://docs.google.com/spreadsheets/d/1-z6Cd-sNzYYnqkYT7BSKEGW9MPgU02dxMf0rRITjQB0/edit

見出し：`受付日時 / パーソナリティ名 / チャンネルURL / 生年月日 / プレゼントに欲しいもの / 誕生日の思い出 / ひとこと`

---

## ステップ1：Apps Script を貼る（このスプシの中で開く）

1. 上のスプレッドシートを開く
2. `拡張機能 → Apps Script` を開く（※必ずこのスプシの中から開くこと。`getActiveSpreadsheet()` がこのシートを指すようになります）
3. 最初のコードを全部消して、下の `gas.js` の中身を貼り付け
4. 💾 保存

## ステップ2：ウェブアプリとしてデプロイ

1. 右上の `デプロイ → 新しいデプロイ`
2. 歯車アイコン → 種類「**ウェブアプリ**」を選択
3. 設定：
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**（※「全員」が必須。これがないと送信できません）
4. `デプロイ` を押し、表示される **ウェブアプリのURL（`https://script.google.com/macros/s/.../exec`）** をコピー

## ステップ3：フォームに送信先をつなぐ

コピーした `.../exec` のURLを共有してもらえれば、`birthday.html` の
`SUBMIT_URL` に組み込みます。（自分でやる場合は `birthday.html` の
`var SUBMIT_URL = "";` の `""` の中にURLを貼るだけ）

---

## gas.js（Apps Scriptに貼るコード）

```javascript
function doPost(e) {
  var data = JSON.parse(e.postData.contents);
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();

  // 見出し行が無ければ自動作成（保険）
  if (sheet.getLastRow() === 0) {
    sheet.appendRow([
      '受付日時', 'パーソナリティ名', 'チャンネルURL', '生年月日',
      'プレゼントに欲しいもの', '誕生日の思い出', 'ひとこと'
    ]);
  }

  sheet.appendRow([
    new Date(),
    data['パーソナリティ名'],
    data['チャンネルURL'],
    data['生年月日'],
    data['プレゼントに欲しいもの'],
    data['誕生日の思い出'],
    data['ひとこと']
  ]);

  return ContentService.createTextOutput('ok');
}
```

---

## 補足

- フォーム(`birthday.html`)のコードを直したあとは、GAS側で
  `デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン「新バージョン」→ デプロイ`
  で再反映が必要です（URLは変わりません）。
- 先方に配る「フォームのURL」は、`birthday.html` をネット上に置いて発行します
  （GitHub Pages など）。希望があれば固定URLまで用意します。
